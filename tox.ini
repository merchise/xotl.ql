[tox]
envlist = py27,pypy

[testenv]
deps = pytest
       Sphinx>=1.3.4,<1.5

commands=py.test -l []
         sphinx-build -d doctest -d docs/build/doctrees docs/source docs/build/doctest

[testenv:py35]
basepython=python3.5


[testenv:py32]
# Sphinx 1.3.6 does not work on Python 3.2.
commands=py.test -l []
